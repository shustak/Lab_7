<Window x:Class="Lab_7.EditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="EditWindow" Height="300" Width="400">
    <Grid x:Name="grid">
        <StackPanel>
            <TextBox x:Name="txtFullName" Text="{Binding FullName}" Margin="5"/>
            <TextBox x:Name="txtAge" Text="{Binding Age}" Margin="5"/>
            <TextBox x:Name="txtPayment" Text="{Binding Payment}" Margin="5"/>
            <TextBox x:Name="txtGroupId" Text="{Binding GroupId}" Margin="5"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="btnOK" Click="btnOK_Click">OK</Button>
                <Button x:Name="btnCancel" Click="btnCancel_Click">Cancel</Button>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
